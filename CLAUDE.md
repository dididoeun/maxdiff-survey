# [원티드 UX 리서치] 설문조사 플랫폼 PRD

---

## Ground Rule

- PRD는 린한 실행(Lean Action)에 맞춰 가설을 검증할 수 있는 최소 단위로 작성합니다.
- 정량적인 지표를 해결할 수 있는 문제 인식, 문제 정의, 가설 설정에 초점을 맞춰 주세요.
- 문제를 해결하는 요구 사항의 시작이자 마지막으로 모든 히스토리를 담습니다.
  - 디자인 혹은 개발 중 논의하는 과정에서 요구 사항이 조금이라도 변경되면 피그마나 지라가 아닌 **이 곳에 업데이트 해 주세요.**

---

## 기본 정보

| 항목 | 내용 |
| --- | --- |
| **진행 상태** | 개발 중 |
| **PRD Jira 링크** | - |
| **담당자** | - |
| **프로덕트/서비스** | 원티드 UX 리서치 |
| **플랫폼** | Desktop/Mobile |
| **기획 완료일** | - |
| **디자인 완료일** | - |
| **개발 완료일** | - |
| **테스트 완료일** | - |
| **배포 완료일** | - |

---

## 배경

UX 리서치팀이 사용자 설문조사를 효율적으로 생성하고, 결과를 정량적으로 분석할 수 있는 내부 도구가 필요합니다. MaxDiff 방법론을 활용해 우선순위를 도출하고, 다양한 문항 타입과 협업 기능을 통해 팀 단위의 리서치 워크플로우를 지원합니다.

---

## 문제 인식

- 리서처
  - 설문 결과를 엑셀로 수작업 분석하여 시간이 많이 소요됨
  - MaxDiff 분석을 위한 별도 외부 툴이 필요하여 비용 및 러닝커브 발생
  - 단일 관리자만 대시보드에 접근 가능해 팀 협업이 어려움
  - MaxDiff 외 일반 문항(객관식, 주관식)을 함께 수집하지 못함
- 응답자
  - 직군별 맞춤 설문 흐름이 없어 무관한 문항에 응답하게 됨
- 원티드랩
  - 리서치 데이터가 단일 담당자에게 집중되어 지식 공유가 어려움

---

## 문제 정의

- MaxDiff 결과 분석에 별도 툴 없이 즉시 시각화 및 분석이 가능해야 한다.
- 여러 리서처가 동일한 설문 대시보드에 접근하여 협업할 수 있어야 한다.
- MaxDiff 외 객관식·주관식 문항을 설문에 함께 포함할 수 있어야 한다.

---

## 가설

- 설문 생성부터 결과 분석까지 원스톱으로 지원하면 리서처의 분석 소요 시간이 50% 이상 감소할 것이다.
- 공동 관리자 기능을 제공하면 팀 내 리서치 결과 공유 빈도가 증가할 것이다.
- 다양한 문항 타입을 지원하면 단일 설문으로 더 풍부한 인사이트를 도출할 수 있을 것이다.

---

## 지표

- 설문 생성 후 결과 분석까지 소요 시간
- 대시보드 접근 사용자 수 (관리자 추가 기능 활용률)
- 설문당 평균 문항 타입 혼합 비율

---

## 해결책

- MaxDiff 알고리즘 기반 설문 생성 및 결과 자동 시각화 제공
- 공동 관리자 초대 기능으로 팀 협업 지원
- 객관식·주관식(단답/장문) 문항 타입 선택 기능 추가

---

## 기술 스택

| 영역 | 기술 |
| --- | --- |
| Frontend | Next.js 15, React 19, TypeScript |
| UI | shadcn/ui (Nova preset), Tailwind CSS v4 |
| Database | Turso (LibSQL / SQLite) |
| Auth | NextAuth.js v5 (Google OAuth, JWT) |
| Charts | Recharts (Bar, Scatter) |

---

## 데이터베이스 스키마

### users
| 컬럼 | 타입 | 설명 |
| --- | --- | --- |
| id | TEXT (PK) | UUID |
| name | TEXT | 이름 |
| email | TEXT (UNIQUE) | 이메일 |
| image | TEXT | 프로필 이미지 |

### accounts
| 컬럼 | 타입 | 설명 |
| --- | --- | --- |
| id | TEXT (PK) | UUID |
| userId | TEXT (FK) | users.id 참조 |
| provider | TEXT | "google" |
| providerAccountId | TEXT | Google 계정 ID |

### surveys
| 컬럼 | 타입 | 설명 |
| --- | --- | --- |
| id | TEXT (PK) | UUID 8자리 |
| title | TEXT | 설문 제목 |
| items | TEXT | JSON 배열 (항목명, 이미지) |
| setSize | INTEGER | 한 화면 항목 수 (기본 4) |
| jobRoles | TEXT | JSON 배열 (직군 선택 목록) |
| userId | TEXT | 설문 생성자 ID |
| status | TEXT | 'draft' \| 'published' |
| createdAt | TEXT | 생성 시간 (ISO 8601) |

### responses
| 컬럼 | 타입 | 설명 |
| --- | --- | --- |
| id | INTEGER (PK, AI) | 자동 증가 |
| surveyId | TEXT (FK) | surveys.id 참조 |
| respondentId | TEXT | 응답자 UUID |
| jobRole | TEXT | 응답자 직군 |
| round | INTEGER | 라운드 번호 |
| setBatch | TEXT | JSON 배열 (세트 항목) |
| bestItem | TEXT | 가장 시급한 항목 |
| worstItem | TEXT | 가장 덜 시급한 항목 |
| createdAt | TEXT | 응답 시간 |

---

## 요구 사항

### Epic 1: Google 계정 기반 인증 및 설문 관리 (완료)

- **Story 1-1**: 리서처는 팀 계정으로 로그인하기 위해 Google OAuth 인증을 사용한다.
  - **Details**:
    - Google 계정으로 로그인 시 users, accounts 테이블에 사용자 정보가 저장된다.
    - JWT 기반 세션이 쿠키에 저장된다.
    - 비로그인 상태에서 `/admin`, `/dashboards`, `/dashboard/*` 접근 시 로그인 페이지로 리다이렉트된다.

- **Story 1-2**: 리서처는 설문을 효율적으로 관리하기 위해 대시보드에서 설문 목록을 확인한다.
  - **Details**:
    - 본인이 생성한 설문만 목록에 표시된다.
    - 각 설문에 응답 수, 생성일, 상태(draft/published)가 표시된다.
    - 응답이 없는 경우에만 설문 수정이 가능하다.
    - 설문 삭제는 항상 가능하다.

---

### Epic 2: MaxDiff 설문 생성 및 발행 (완료)

- **Story 2-1**: 리서처는 우선순위 조사를 위해 MaxDiff 설문을 생성한다.
  - **Details**:
    - 설문 제목, 평가 항목(이름+이미지), 세트 크기(2~8), 직군 목록을 입력할 수 있다.
    - 임시저장(draft) 및 발행(published) 두 가지 상태로 저장할 수 있다.
    - 발행 시 응답자용 고유 URL이 생성된다.

- **Story 2-2**: 응답자는 MaxDiff 설문에 참여하기 위해 응답자용 URL에 접속한다.
  - **Details**:
    - 직군 선택 후 설문이 시작된다 (직군 설정이 없으면 바로 시작).
    - BIB 디자인 기반으로 각 항목이 최소 3회 이상 노출되도록 세트가 자동 생성된다.
    - 각 라운드에서 "가장 시급한 것"과 "가장 덜 시급한 것"을 선택한다.
    - 같은 항목을 Best/Worst로 중복 선택할 수 없다.
    - 모든 라운드 완료 후 완료 화면이 표시된다.

---

### Epic 3: 설문 결과 분석 대시보드 (완료)

- **Story 3-1**: 리서처는 우선순위를 파악하기 위해 MaxDiff 점수를 시각화하여 확인한다.
  - **Details**:
    - 항목별 MaxDiff 점수가 막대 차트로 표시된다.
    - 점수는 0~100 사이로 정규화된다 (rawScore = (best - worst) / exposure 기준).
    - 순위, Best/Worst 횟수, 노출 횟수, 점수가 테이블로 제공된다.
    - CSV 다운로드 기능이 제공된다.

- **Story 3-2**: 리서처는 우선순위-사용빈도 매트릭스 분석을 위해 외부 데이터를 업로드한다.
  - **Details**:
    - CSV 파일 업로드로 사용 빈도 데이터를 입력할 수 있다.
    - X축(사용 빈도) vs Y축(MaxDiff 점수)의 2×2 매트릭스 산점도가 표시된다.
    - 4개 사분면(중점 개선 / 정리·재설계 / 안정 / 저우선순위)으로 항목을 분류한다.

---

### Epic 4: 설문 공동 관리자 기능 (예정)

- **Story 4-1**: 설문 생성자는 팀원과 결과를 공유하기 위해 공동 관리자를 초대한다.
  - **Details**:
    - 이메일로 공동 관리자를 초대할 수 있다.
    - 초대받은 사용자는 로그인 후 해당 설문 대시보드에 접근할 수 있다.
    - 공동 관리자는 결과 조회만 가능하며 설문 수정·삭제는 생성자만 가능하다.
    - 설문 대시보드에 공동 관리자 목록이 표시된다.
    - 생성자는 공동 관리자를 제거할 수 있다.

- **Story 4-2**: 공동 관리자는 초대를 수락하기 위해 이메일 링크를 통해 접근한다.
  - **Details**:
    - 초대 이메일에 수락 링크가 포함된다.
    - 링크 클릭 시 로그인 후 공동 관리자로 등록된다.
    - 초대 링크는 7일 후 만료된다.
    - 이미 등록된 관리자를 중복 초대할 수 없다.

#### 추가 필요 DB 스키마

**survey_admins 테이블** (신규)
| 컬럼 | 타입 | 설명 |
| --- | --- | --- |
| id | TEXT (PK) | UUID |
| surveyId | TEXT (FK) | surveys.id 참조 |
| userId | TEXT (FK) | users.id 참조 (수락한 경우) |
| email | TEXT | 초대 이메일 |
| token | TEXT | 초대 토큰 |
| status | TEXT | 'pending' \| 'accepted' |
| expiresAt | TEXT | 만료 시간 |
| createdAt | TEXT | 초대 시간 |

---

### Epic 5: 다양한 문항 타입 지원 (예정)

- **Story 5-1**: 리서처는 더 풍부한 데이터 수집을 위해 설문에 다양한 문항 타입을 추가한다.
  - **Details**:
    - 설문 생성 시 문항 타입을 선택할 수 있다: MaxDiff, 객관식, 주관식(단답), 주관식(장문).
    - MaxDiff 문항과 일반 문항은 하나의 설문 내에 함께 구성할 수 있다.
    - 문항 순서를 드래그로 변경할 수 있다.
    - 각 문항 타입별 입력 UI가 구분되어 표시된다.

- **Story 5-2**: 응답자는 설문 흐름에 따라 다양한 문항 타입에 응답한다.
  - **Details**:
    - 객관식 문항은 단일 선택 또는 복수 선택을 지원한다.
    - 주관식(단답)은 한 줄 텍스트 입력, 주관식(장문)은 여러 줄 텍스트 입력이 제공된다.
    - 필수 문항 미응답 시 다음 문항으로 넘어가지 않는다.

- **Story 5-3**: 리서처는 일반 문항 응답 결과를 대시보드에서 확인한다.
  - **Details**:
    - 객관식 응답은 선택지별 응답 수와 비율(%)로 표시된다.
    - 주관식 응답은 응답 목록으로 표시된다.
    - MaxDiff 결과와 일반 문항 결과가 대시보드에서 탭으로 구분된다.

#### 추가 필요 DB 스키마

**questions 테이블** (신규)
| 컬럼 | 타입 | 설명 |
| --- | --- | --- |
| id | TEXT (PK) | UUID |
| surveyId | TEXT (FK) | surveys.id 참조 |
| type | TEXT | 'maxdiff' \| 'multiple_choice' \| 'short_answer' \| 'long_answer' |
| title | TEXT | 문항 내용 |
| options | TEXT | JSON 배열 (객관식 선택지, maxdiff 항목 포함) |
| required | INTEGER | 필수 여부 (0/1) |
| order | INTEGER | 문항 순서 |

**general_responses 테이블** (신규)
| 컬럼 | 타입 | 설명 |
| --- | --- | --- |
| id | INTEGER (PK, AI) | 자동 증가 |
| surveyId | TEXT (FK) | surveys.id 참조 |
| questionId | TEXT (FK) | questions.id 참조 |
| respondentId | TEXT | 응답자 UUID |
| answer | TEXT | 응답 내용 (JSON 또는 텍스트) |
| createdAt | TEXT | 응답 시간 |

---

## 향후 방향

- 설문 응답 알림 기능 (이메일/슬랙 연동)
- 설문 템플릿 기능 (자주 쓰는 설문 구성 저장)
- 응답자 세그먼트별 필터링 분석
- 반응형 모바일 최적화

---

## 고려 사항

- **데이터 마이그레이션**: Epic 5 적용 시 기존 surveys.items 컬럼 데이터를 questions 테이블로 마이그레이션 필요
- **하위 호환성**: 기존 MaxDiff 전용 설문의 응답 데이터 구조 유지 필요
- **권한 분리**: 공동 관리자는 조회 전용, 수정·삭제는 생성자 전용으로 엄격히 분리
- **초대 보안**: 초대 토큰은 일회성 사용 및 만료 시간 설정 필수
